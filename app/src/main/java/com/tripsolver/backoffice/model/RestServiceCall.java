package com.tripsolver.backoffice.model;

/**
 * Created by lenovo on 4/24/2019.
 */

public class RestServiceCall {


/*

    Call<TicketBookingsResponse> call = apiService.getTopRatedMovies("");
        call.enqueue(new Callback<TicketBookingsResponse>() {
        @Override
        public void onResponse(Call<TicketBookingsResponse>call, Response<TicketBookingsResponse> response) {
            List<Movie> movies = response.body().getResults();
            Log.d(TAG, "Number of movies received: " + movies.size());
        }

        @Override
        public void onFailure(Call<TicketBookingsResponse>call, Throwable t) {
            // Log error here since request failed
            Log.e(TAG, t.toString());
        }
    });
*/


}
